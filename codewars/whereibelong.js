function whereIBelong(arr, num) {
    let counter = 0

    for (let index = 0; index < array.length; index++) {
        if (num > arr[i]) {
            counter++
        }
        
    }
    return counter
}